data_dir: data
output_dir: checkpoints
tokenizer_path: data/tokenizer.json
model_path: null
seed: 42
device: auto
dtype: bf16
batch_size: 4
grad_accum: 8
lr: 1.0e-5
weight_decay: 0.0
betas: [0.9, 0.95]
warmup_steps: 20
max_steps: 5000
epochs: 1
max_seq_len: 512
grad_clip: 1.0
log_every: 10
save_every: 500
max_samples: null
eval_every: 0
use_logger: none
run_name: dpo
beta: 0.1
ref_model_path: null
