data_dir: data
output_dir: checkpoints
tokenizer_path: data/tokenizer.json
model_path: null
seed: 42
device: auto
dtype: bf16
batch_size: 8
grad_accum: 4
lr: 1.0e-4
weight_decay: 0.1
betas: [0.9, 0.95]
warmup_steps: 50
max_steps: 20000
epochs: 1
max_seq_len: 512
grad_clip: 1.0
log_every: 10
save_every: 500
max_samples: null
eval_every: 0
use_logger: none
run_name: sft
