data_dir: data
output_dir: checkpoints
tokenizer_path: data/tokenizer.json
model_path: null
seed: 42
device: auto
dtype: bf16
batch_size: 4
grad_accum: 1
lr: 1.0e-5
weight_decay: 0.0
betas: [0.9, 0.95]
warmup_steps: 20
max_steps: 2000
epochs: 1
max_seq_len: 512
grad_clip: 1.0
log_every: 5
save_every: 200
max_samples: null
eval_every: 0
use_logger: none
run_name: ppo
rm_path: null
ref_model_path: null
response_max_len: 128
temperature: 1.0
top_p: 0.9
top_k: 50
gamma: 0.99
lam: 0.95
clip_eps: 0.2
kl_coef: 0.1
ppo_epochs: 1
ent_coef: 0.01
vf_coef: 1.0
n_rollout_prompts: 2000
